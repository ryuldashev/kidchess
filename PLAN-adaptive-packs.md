# Адаптивные паки для Kids Mode

## Цель
После прохождения 9 уровней в паке — генерировать новые 9, фокусируясь на слабых местах ребёнка.

## Ключевые решения
- **Источник задач:** Генератор позиций (алгоритм создаёт FEN на лету)
- **Режимы:** Только Kids Mode (pack1, pack2, pack3)
- **Метрики:** Неиспользуемые фигуры + паттерны направлений
- **Конь на 3x3:** Работает (g1→f3), как в текущих задачах
- **UI статистики:** Экран "Прогресс" для родителей

---

## Архитектура

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  ErrorTracker   │────▶│  PuzzleGenerator │────▶│   Game.js       │
│  (сбор данных)  │     │  (генерация FEN) │     │  (интеграция)   │
└─────────────────┘     └──────────────────┘     └─────────────────┘
         │                                                │
         ▼                                                ▼
┌─────────────────┐                              ┌─────────────────┐
│  localStorage   │                              │  ParentStats UI │
│  kidChessStats  │                              │  (экран для     │
└─────────────────┘                              │   родителей)    │
                                                 └─────────────────┘
```

---

## Файлы для создания

### 1. `js/error-tracker.js` (новый)
Модуль сбора и анализа ошибок:
- `init()` — загрузка из localStorage
- `recordSuccess(piece, from, to)` — успешный ход
- `recordError(piece, from, to, legalMoves)` — ошибка + анализ типа
- `getWeaknesses()` — список слабых мест с приоритетами
- `getRecommendedPiece()` — какую фигуру использовать в следующей задаче

**Структура данных:**
```javascript
kidChessStats: {
    pieceStats: {
        r: { attempts: 15, errors: 2, lastUsed: timestamp },
        b: { attempts: 10, errors: 5, lastUsed: timestamp },
        n: { attempts: 8, errors: 4, lastUsed: timestamp },
        q: { attempts: 12, errors: 1, lastUsed: timestamp }
    },
    directionErrors: {
        diagonal: 3,    // ладья пыталась ходить по диагонали
        vertical: 1,    // слон по вертикали
        horizontal: 2,  // слон по горизонтали
        knight: 5       // неправильные L-ходы
    },
    totalMoves: 45,
    totalErrors: 12,
    generatedLevels: { pack1: [...], pack2: [...] }
}
```

### 2. `js/puzzle-generator.js` (новый)
Генератор FEN-позиций:
- `getBoardSquares(boardSize)` — допустимые клетки для размера доски
- `generatePosition({ boardSize, whitePiece, blackPieces })` — создание позиции
- `generateLevelSet(packConfig, count=9)` — набор из 9 уровней
- `generateHint(piece)` — подсказка для фигуры

**Логика генерации:**
1. Выбрать белую фигуру (с учётом слабых мест)
2. Разместить её на допустимой клетке
3. Найти клетки которые она атакует
4. Разместить туда 1-3 чёрные пешки
5. Собрать FEN

---

## Файлы для модификации

### 3. `js/game.js`
**Изменения:**

| Функция | Изменение |
|---------|-----------|
| `init()` | Добавить `ErrorTracker.init()` |
| `handleKidsCellClick()` | Вызывать `recordSuccess()` / `recordError()` |
| `loadKidsLevel(index)` | Если index >= levels.length → вызвать генерацию |
| `loadProgress()` | Восстанавливать сгенерированные уровни |

**Новые методы:**
- `generateNewKidsLevels(pack)` — вызов генератора
- `showNewLevelsNotification()` — UI уведомление "Новые задачи!"
- `showParentStats()` — экран статистики для родителей

### 4. `index.html`
- Подключить `js/error-tracker.js`
- Подключить `js/puzzle-generator.js`
- Добавить кнопку "Прогресс" (иконка в углу)

### 5. `css/style.css`
- Стили для badge "Новые задачи!"
- Стили для экрана статистики родителей

---

## UI: Экран статистики для родителей

```
┌────────────────────────────────────────┐
│  📊 Прогресс                      ✕    │
├────────────────────────────────────────┤
│                                        │
│  Пройдено уровней: 27                  │
│  Сгенерировано новых: 18               │
│                                        │
│  ─── Фигуры ───                        │
│  ♖ Ладья     ████████░░  80%           │
│  ♗ Слон      ████░░░░░░  40%  ⚠️       │
│  ♘ Конь      ██████░░░░  60%           │
│  ♕ Ферзь     █████████░  90%           │
│                                        │
│  ─── Над чем работаем ───              │
│  • Слон: путает с ладьёй               │
│  • Конь: сложные L-ходы                │
│                                        │
└────────────────────────────────────────┘
```

Доступ: кнопка 📊 в header или долгий тап на эмодзи пака.

---

## Порядок реализации

### Фаза 1: Сбор данных (основа)
1. Создать `js/error-tracker.js`
2. Интегрировать в `handleKidsCellClick()`
3. Подключить в `index.html`
4. Тест: проверить что данные сохраняются

### Фаза 2: Генератор
1. Создать `js/puzzle-generator.js`
2. Реализовать генерацию для каждой фигуры
3. Тест: сгенерировать 10 позиций, проверить валидность через chess.js

### Фаза 3: Интеграция
1. Модифицировать `loadKidsLevel()` — триггер генерации
2. Добавить восстановление сгенерированных уровней
3. Добавить UI уведомление "Новые задачи!"

### Фаза 4: UI статистики
1. Добавить кнопку доступа
2. Создать модальное окно статистики
3. Визуализация прогресса по фигурам

---

## Технические детали

### Размер доски → допустимые клетки
- `boardSize: 3` → f1-h3 (9 клеток)
- `boardSize: 4` → e1-h4 (16 клеток)

### Пример генерации FEN
Вход: `{ boardSize: 3, whitePiece: 'r', blackPieces: 2 }`
1. Белая ладья на h1
2. Ладья атакует: h2, h3, g1, f1
3. Выбираем 2 клетки: h3, f1
4. FEN: `8/8/8/8/8/7p/8/5p1R w - - 0 1`

### Fallback при ошибке генерации
Если не удалось сгенерировать валидную позицию (редкий случай):
- Повторить с другими параметрами
- Если 3 неудачи — вернуть index=0 (начало пака)

---

## Критические файлы

| Файл | Действие |
|------|----------|
| `js/error-tracker.js` | Создать |
| `js/puzzle-generator.js` | Создать |
| `js/game.js` | Модифицировать |
| `index.html` | Добавить скрипты + кнопку |
| `css/style.css` | Добавить стили |
