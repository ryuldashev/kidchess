# План: Улучшение подсказок и конфетти в KidChess

## Задачи

### 1. Умные подсказки (динамические)
**Файлы:** `js/game.js`, `js/ai.js`

Вместо статичных `hint: ['♖', '↑']` в puzzles.js — вычислять в реальном времени:

- Новая функция `calculateSmartHint()` — вызывает `ChessAI.getBestMove(this.chess, 3)`
- Новая функция `getDirection(from, to)` — конвертирует координаты в стрелку
- Маппинг фигур: `{r: '♖', n: '♘', b: '♗', q: '♕', k: '♔', p: '♙'}`
- Маппинг направлений: `{0,1: '↑', 1,1: '↗', 1,0: '→', ...}`

**Изменить:** `showKidsHint()` — вызывать `calculateSmartHint()` вместо `level.hint`

---

### 2. Чат-поведение подсказок
**Файлы:** `index.html`, `js/game.js`, `css/style.css`

#### HTML:
Добавить контейнер `#hint-container` для истории сообщений

#### JS — логика `addHintMessage(symbols, type)`:
1. Найти старые сообщения в контейнере
2. Добавить им класс `fading-out`
3. Удалить через 500ms
4. Создать новый бабл через DOM методы (createElement, appendChild)
5. Добавить аватар 🦊 и символы подсказки

#### CSS — анимации:
- `slideInFromBottom` — новое сообщение появляется снизу
- `slideUpAndFade` — старое уходит вверх с fade out

---

### 3. Конфетти на полэкрана
**Файлы:** `js/game.js`, `css/style.css`

#### JS — изменить `launchConfetti()`:
- Увеличить количество: 30 → 60
- Расширить стартовую область: top 30-70% (не только центр)
- Увеличить время жизни: 2000ms → 3000ms

#### CSS — изменить `@keyframes confettiFall`:
- Увеличить высоту полёта: -80px → -200px
- Добавить разброс по горизонтали

---

## Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `js/game.js` | `calculateSmartHint()`, `getDirection()`, `addHintMessage()`, `launchConfetti()` |
| `css/style.css` | Анимации чата, конфетти высота |
| `index.html` | Контейнер `#hint-container` |

## Порядок выполнения

1. Умные подсказки — `calculateSmartHint()` + `getDirection()`
2. Чат-поведение — HTML контейнер + JS логика + CSS анимации
3. Конфетти — расширить область + увеличить количество + высота полёта
4. Тестирование в Kids Mode
